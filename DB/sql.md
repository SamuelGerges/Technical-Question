## Database SQL

## View
    it  is a virtual table based on a SQL query. 
    It does not store data, but stores the SQL logic, and when queried, 
    it returns dynamic results from the underlying tables.
    - Updatable Views: Views that allow INSERT, UPDATE, DELETE operations under certain condition
    - Indexed Views: Views with physical indexes for performance optimization (SQL Server)
       
## 
